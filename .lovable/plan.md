

# Remover ícone CalendarClock do badge D-X

Remover o ícone `<CalendarClock>` do componente `PreviousDayBadge` em `src/components/dashboard/CarregamentoTable.tsx` (linha 48), mantendo apenas o texto "D-{diff}".

Também remover o import de `CalendarClock` se não for usado em outro lugar do arquivo.

